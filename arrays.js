//arrays

let array = new Array();
let arrays =[];
arrays=["123","127","124"];
arrays.push("ray");
arrays.pop();
arrays.shift();
arrays.unshift("ray");
console.log(arrays);
console.log(arrays.length());

for(iter of arrays)
{
    console.log(iter);
}
